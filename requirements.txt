# Dependencias del recolector OPC UA de FROXA.
#
# Instalacion recomendada dentro de un entorno virtual:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# asyncua es el cliente OPC UA asincrono que usa el servicio
# (Client, suscripciones, lectura de nodos...).
asyncua>=1.1,<2.0
python-dotenv>=1.2,<2.0
psycopg[binary]>=3.3,<4.0